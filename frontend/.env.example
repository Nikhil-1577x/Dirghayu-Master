# Local development: leave empty — Vite proxies /patient, /iot, /ws, /health to the backend (see vite.config.ts).
# Setting this to http://localhost:8000 forces cross-origin requests and requires backend CORS; avoid unless needed.
# VITE_API_URL=
