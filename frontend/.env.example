# Local development: leave empty — dev mode uses http://127.0.0.1:8000 for API + WebSockets (see src/api/client.ts).
# Override if your backend is elsewhere, e.g. VITE_API_URL=http://192.168.1.5:8000
# VITE_API_URL=
